# v0.4.0 Community Adapters Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 GitHub、Hacker News、Reddit 与 Techmeme 四类公开社区 Adapter，并接入现有 Runtime、doctor 与 shadow 采集。

**Architecture:** 每个 Adapter 独占来源解析和子流状态，复用受限 HTTP client，输出 `SourceCandidate` 和待提交 checkpoint；跨来源路由、批次与失败隔离仍由现有 Runtime/Node 消费层负责。

**Tech Stack:** Python 3.12、unittest、公开 REST/JSON/RSS/HTML Fixture。

**Requires:** `2026-09-15-v0.4.0-acquisition-foundation.md`

---

## Chunk 1: GitHub 与 Hacker News

### Task 0: 扩展受限 HTTP client 支持 JSON POST

**Files:**
- Modify: `src/follow_up_acquisition/http_client.py`
- Modify: `tests/acquisition/test_http_client.py`

- [x] **Step 1: 写失败测试**：受限 `post_json` 复用 pinned DNS/Host/SNI/peer/redirect/timeout/body 上限，不接受 caller Host/Content-Length/Transfer-Encoding；请求 JSON canonical、大小受限，307/308 才可保留 POST，301/302/303 不自动改写方法，429/响应截断不重试。
- [x] **Step 2: RED**：运行 HTTP client 测试，确认 POST API 尚不存在。
- [x] **Step 3: 最小实现**：传输层显式接收 method/body；`post_json` 只发送 `application/json`，在序列化前限制 exact JSON 形状、深度、节点与 UTF-8 bytes；GET 行为字节兼容。
- [x] **Step 4: GREEN/回归**：运行 HTTP、Runtime、GitHub 预备测试与 Python 全量测试。
- [x] **Step 5: 审查与提交**：两阶段审查后提交 `feat: add restricted JSON POST transport`。

### Task 1: GitHub Adapter

**Files:**
- Create: `src/follow_up_acquisition/adapters/github.py`
- Create: `src/follow_up_acquisition/adapters/github_models.py`
- Create: `src/follow_up_acquisition/adapters/github_executor.py`
- Create: `src/follow_up_acquisition/adapters/github_scheduler.py`
- Create: `src/follow_up_acquisition/adapters/github_queries.py`
- Create: `src/follow_up_acquisition/adapters/github_rest.py`
- Create: `src/follow_up_acquisition/adapters/github_releases.py`
- Create: `src/follow_up_acquisition/adapters/github_discussions.py`
- Create: `src/follow_up_acquisition/adapters/github_mapping.py`
- Create: `tests/acquisition/test_github_adapter.py`
- Create: `tests/acquisition/test_github_budget.py`
- Create: `tests/acquisition/test_github_executor.py`
- Create: `tests/acquisition/test_github_rest.py`
- Create: `tests/acquisition/test_github_releases.py`
- Create: `tests/acquisition/test_github_discussions.py`
- Create: `tests/acquisition/fixtures/github/`

- [x] **Step 1: 写失败 Fixture 测试**：Repository、Release、Commit、Issue、PR、Discussion；分页、window、budget、query fingerprint、node ID、缺 node ID、304、匿名限流、Token 失效、Discussion 缺权限和部分查询失败。
- [x] **Step 2: RED**：运行 `tests.acquisition.test_github_adapter`，预期模块不存在。
- [x] **Step 3: 实现有界执行器与调度**：拆分 models/executor/scheduler/query 模块；匿名请求硬上限 24/9/15/0，Token 为 64/27/31/6；每个 HTTP call 一个 quantum；lane 只来自 enabled entities，scheduler stream 以 lane-set fingerprint/next lane 跨运行公平恢复；rate/auth breaker 按冻结优先级立即停止后续调用。
- [x] **Step 4: 实现 REST 与 Release 路径**：构造器统一接受 `resolve_source`、injected HTTP client/clock、credential/checkpoint resolver；Token 与 Discussion 开关无关且只解析一次。REST envelope/page10/window 严格；Release 使用不含 pushed 的 bounded 100-repo roster 与 parent/page cursor；parent lookup 服从 core budget。
- [x] **Step 5: 实现 Discussion 与 mapping**：GraphQL pageInfo/query-set fingerprint 严格；固定 `api.github.com` allowlist；entity identity 使用 `github:<type>:<node-id>`，Commit 使用 repo node ID + lowercase SHA；mapping/URL/time/metrics/provenance 关闭字段，Fixture 零网络。
- [x] **Step 6: 实现状态矩阵**：严格区分 ok/no-results/partial/rate-limited/auth-failed/schema-drift/error；预算耗尽、breaker、lane failure 均保留成功进度，失败 lane 不推进；输出 platform community metrics、query/discussions/scheduler checkpoint updates。
- [x] **Step 7: GREEN/回归**：运行 GitHub facade、budget、REST、Release、Discussion、HTTP、state、runtime、redaction 测试；断言匿名/Token 请求数硬上限和连续运行 lane 收敛。
- [x] **Step 8: 提交**：`feat: add GitHub acquisition adapter`。

### Task 2: Hacker News Adapter

**Files:**
- Create: `src/follow_up_acquisition/adapters/hackernews.py`
- Create: `tests/acquisition/test_hackernews_adapter.py`
- Create: `tests/acquisition/fixtures/hackernews/`

- [x] **Step 1: 写失败 Fixture 测试**：Firebase top/new ID 列表和 item；Algolia 时间窗搜索；story/comment 区分；points/descendants/rank；分页、query reorder、空+失败 partial、全部空 no-results。
- [x] **Step 2: RED**：运行 HN 测试，预期模块不存在。
- [x] **Step 3: 最小实现**：构造器接受 `resolve_source`、injected HTTP client/clock；固定 `hacker-news.firebaseio.com` 与 `hn.algolia.com`；仅 story 形成候选；comments 只做有预算 enrichment；identity 为 HN item ID；Fixture 断言零真实网络。
- [x] **Step 4: 子流 checkpoint**：`top`、`new`、`search.<query-id>` 独立推进；失败子流保持旧水位。
- [x] **Step 5: GREEN/回归**：运行 HN、HTTP、state、runtime 测试。
- [x] **Step 6: 提交**：`feat: add Hacker News acquisition adapter`。

## Chunk 2: Reddit 与 Techmeme

### Task 3: Reddit 免 Key Adapter

**Files:**
- Create: `src/follow_up_acquisition/adapters/reddit.py`
- Create: `tests/acquisition/test_reddit_adapter.py`
- Create: `tests/acquisition/fixtures/reddit/`

- [x] **Step 1: 写失败 Fixture 测试**：RSS、公开 listing JSON、缺指标 warning、fullname identity、重复、时间窗、登录墙、robots/访问拒绝、429、RSS 失败后 listing 回退和全部路径失败。
- [x] **Step 2: RED**：运行 Reddit 测试，预期模块不存在。
- [x] **Step 3: 实现 RSS 主路径**：构造器接受 `resolve_source`、injected HTTP client/clock；固定 source subreddit 与 `www.reddit.com` host；只读取 post 元数据，不抓评论全文或用户资料；Fixture 断言零真实网络。
- [x] **Step 4: 实现受限 listing 回退**：只对同一 subreddit 启用；RSS 成功即不请求回退；HTML 登录墙不做规避。
- [x] **Step 5: 状态与 metrics**：identity 使用 fullname；保留 score/upvote ratio/comments/flair/rank；按 spec 映射错误。
- [x] **Step 6: GREEN/回归**：运行 Reddit、RSS、HTTP、redaction 测试。
- [x] **Step 7: 提交**：`feat: add keyless Reddit acquisition adapter`。

### Task 4: Techmeme Adapter

**Files:**
- Create: `src/follow_up_acquisition/adapters/techmeme.py`
- Create: `tests/acquisition/test_techmeme_adapter.py`
- Create: `tests/acquisition/fixtures/techmeme/`

- [x] **Step 1: 写失败 Fixture 测试**：首页/日期归档、稳定 anchor、无 anchor fallback、lead/related links、页面时区、14 日归档 checkpoint、部分日期失败、全部布局漂移。
- [x] **Step 2: RED**：运行 Techmeme 测试，确认模块缺失。
- [x] **Step 3: 实现 HTML parser**：构造器接受 `resolve_source`、injected HTTP client/clock；固定 `www.techmeme.com`；原始 lead URL 为 candidate URL，Techmeme cluster URL 写 provenance；解析器不执行脚本；Fixture 断言零真实网络。
- [x] **Step 4: 实现归档窗口**：`front` 与单一 `archive` 子流；checkpoint 仅保留 14 个完整日期；失败日期不标记完成。
- [x] **Step 5: GREEN/回归**：Techmeme、HTTP、state、runtime 测试通过。
- [x] **Step 6: 提交**：`feat: add Techmeme acquisition adapter`。

## Chunk 3: 集成

### Task 5: 接入 collect、doctor 与 core-topic routing

**Files:**
- Modify: `src/follow_up_acquisition/collect.py`
- Modify: `src/follow_up_acquisition/cli.py`
- Modify: `scripts/lib/route-channels.js`
- Modify: `scripts/lib/load-signal-batches.js`
- Modify: `scripts/lib/local-candidate-store.js`
- Modify: `scripts/lib/resolve-acquisition-input.js`
- Modify: `scripts/collect-and-prepare.js`
- Modify: `scripts/digest-candidates.js`
- Modify: `scripts/prepare-digest.js`
- Modify: `scripts/finalize-digest.js`
- Modify: `scripts/delivery-message.js`
- Modify: `config/config-schema.json`
- Modify: `scripts/config-contract.js`
- Create: `contracts/review-candidates.schema.json`
- Create: `scripts/lib/review-queue.js`
- Create: `scripts/test/review-queue.test.js`
- Modify: `contracts/candidate-feed.schema.json`
- Modify: `contracts/digest-curation-request.schema.json`
- Modify: `scripts/candidate-feed-contract.js`
- Modify: `scripts/digest-selection-contract.js`
- Modify: `tests/acquisition/test_collect.py`
- Modify: `tests/acquisition/test_cli.py`
- Modify: `scripts/test/route-channels.test.js`
- Modify: `scripts/test/load-signal-batches.test.js`
- Modify: `scripts/test/local-candidate-store.test.js`
- Modify: `scripts/test/resolve-acquisition-input.test.js`
- Modify: `scripts/test/collect-and-prepare.test.js`
- Modify: `scripts/test/digest-candidates.test.js`
- Modify: `scripts/test/prepare-digest.test.js`
- Modify: `scripts/test/finalize-digest.test.js`
- Modify: `scripts/test/delivery-message.test.js`
- Modify: `scripts/test/config-contract.test.js`
- Modify: `scripts/test/candidate-feed-contract.test.js`
- Modify: `scripts/test/digest-selection-contract.test.js`

- [x] **Step 1: 写失败集成测试**：四 Adapter 被构造；`--source` 可独立运行；一个失败不阻塞其他；doctor availability 为 ok；同一 batch 的不同候选可按标题/摘要/query provenance 路由到不同合法频道；未知主题只进入 review queue，绝不进入 Candidate Feed。
- [x] **Step 2: RED**：运行定向 Python/Node 测试，确认缺 wiring。
- [x] **Step 3: 实现 Adapter wiring**：通过 adapter factory 拆分 `collect.py`，避免继续扩大条件链；不改变 central/shadow/hybrid/local 语义。
- [x] **Step 4: 实现逐候选路由**：`mapSignalBatchItem` 将候选内容/provenance 传给 router，而不是为整批只算一次 channel；fixed source 仍固定。`loadSignalBatches` 返回 `candidates` 与 `reviewCandidates` 两组。
- [x] **Step 5: 建模多频道 source status**：Candidate Feed 与 curation request 的 sourceStatus 保持 1.0 向后兼容：fixed source 继续使用非空 `channel`；core-topic source 使用 `channel: null` + 必填、唯一、按 canonical channel 顺序的 `channels`（可为空）。validator 要求每个正式 candidate.channel 出现在其 source status channels；来源仍只有一个 status，不复制 source ID。完整性按来源状态计算，频道展示按 `channels` 投影。`resolve-acquisition-input.failureStatus()` 从 canonical source policy 构造状态，禁止把 core-topic 缺批次/回滚来源回退成 `blogs`。
- [x] **Step 6: 贯穿 eligibility/completeness**：enabled-channel 先按 candidate.channel 过滤正式候选。core-topic source status 在 `channels` 与 enabledChannels 有交集时保留；`channels: []` 的 no-results/partial/error 也必须保留，因为该来源可能影响任一已启用频道。expected/missing source 计算对任一非空 enabledChannels 都包含已启用 core-topic source，不按 null channel 丢弃；只有用户 `enabledChannels: []` 时走既有 no-channels。`digest-candidates` 与 `prepare-digest` 使用同一 helper，避免过滤规则复制。
- [x] **Step 7: 贯穿最终 artifact**：`incompleteSources` 对 fixed source 保持原 `channel`；core-topic 使用 `channel: null` + canonical `channels`，无候选失败时允许空数组。`finalize-digest` 精确复制该形状，`delivery-message` validator 与 renderer 接受且只展示来源名，不虚构频道；旧 1.0 fixed-source artifact Fixture 继续通过。
- [x] **Step 8: 实现独立 review queue**：严格 1.0 Schema 只含 candidate identity、source、标题、URL、first/last seen 和 `reason=unclassified-core-topic`；`collect-and-prepare.js` 在非 central 模式把 `reviewCandidates` 交给 queue writer，原子写入 `~/.follow-builders/acquisition/review-candidates.json`。review candidate 不使用 channel、不参与 Digest/Feed/source status candidateCount；central 模式不创建该文件。
- [x] **Step 9: GREEN**：定向 Node 103/103、Python 全量 522/522 与 Adapter availability 通过；真实来源 smoke、shadow 和发布门禁另行保留。
- [ ] **Step 10: 审查与提交**：两阶段审查后提交为 `feat: integrate v0.4 community sources`。
